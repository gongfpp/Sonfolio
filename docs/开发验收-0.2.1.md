# 开发验收 0.2.1（数据一致性版本）

日期：2026-09-15。设备：Redmi Note 8 Pro（begonia / Android 10，MIUI），TCP ADB 真机。
主机侧（编译、JVM 单测、Lint、Release 构建、CI run 34921307355 全绿）不作为真机通过的证据，下列全部为真机结果。

## 安装与数据迁移

- 覆盖安装前核对：录音服务未运行、capture-journal 已清空；按规则备份数据库快照与 32 份原音 SHA-256 清单。
- 0.2.0（code 10）→ 0.2.1（code 11）覆盖安装成功。
- Schema 5→8 迁移真机验证：`PRAGMA user_version=8`，conversations 含 generatedTitle/titleOverride/localStartDate，audio_chunks 含 recordedZoneId/recordedOffsetSeconds/localStartDate/compressedPath/compressedBytes；11 对话、318 转写完整，`integrity_check=ok`。

> 事件说明：验收期间主应用曾被 AGP/UTP 测试安装器在 MIUI「USB 安装确认」拦截（INSTALL_FAILED_USER_RESTRICTED）后的清理逻辑连带卸载，数据随应用目录删除。日志证据：`MIUILOG INSTALL_FAILED_USER_RESTRICTED` + `MIUILOG-onPackageDeleted`。真机测试以后一律手动 `adb install` 两个 APK 后用 `am instrument` 执行，不再走 gradle 安装流程。用户已确认重装、旧数据不恢复（数据库快照与原音哈希仍存档于 QA 备份）。

## 真机 instrumented 测试（am instrument 直接执行）

- ConversationEditIntegrationTest：OK（P0① 标题/备注所有权）
- RecordingZoneIntegrationTest：OK（P1① 时区归属）
- AudioCompressionIntegrationTest：OK（P1② 压缩可播放）
- BackupIntegrationTest：OK（P0③ v1 备份迁移 + 同版本 RoundTrip）

## 真机手测

- 全新安装后 Schema 8 直接建立，首页正常渲染（新文案「你的记录保存在本机 · 按日期回看」）。
- 短录音 46 秒：切片落库 `recordedZoneId=Asia/Shanghai`、`recordedOffsetSeconds=28800`、`localStartDate=2026-09-15`；VAD 推进到 `VAD_READY`；无缺口；capture-journal 清空。
- 处理链路停于 VAD_READY 属预期（ASR 模型在卸载事件中丢失，需重新下载后再验收 ASR→整理→压缩全链）。

## 过程中发现并修复的缺陷

- `MemoryBackup`：`JSONObject.NULL` 经 `optString` 变成字面量 `"null"`，导致 WAV 备份被误判为 m4a 分支（真机 BackupIntegrationTest 首跑暴露）。已修复并用两个用例验证。
- `ConversationEditIntegrationTest` 断言错误（清空备注后误期望备注保留），已修正为「清空不得被 rebuild 带回」。

## 待办

- 手机重新下载 ASR/总结模型后：验收 ASR→ASSEMBLY→Conversation→压缩全链与保留策略清理。
- P0④ 长时间（8–12 小时）真机压力验收另行安排。
