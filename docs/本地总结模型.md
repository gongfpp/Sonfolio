# 本地总结模型准备

2026 年 9 月 14 日，用户授权下载此前暂缓的手机本地总结模型。首个验收候选选用 [Qwen 官方 Qwen2.5-0.5B-Instruct-GGUF](https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct-GGUF) 的 `Q4_K_M` 版本：它是支持中文的约 0.49B 参数指令模型，提供 GGUF 格式，使用 Apache-2.0 许可，适合先以较小资源开销验证当前 llama.cpp 接入。它不是语音识别模型，不替换 Silero VAD 或 SenseVoice；下载完成也不等于总结质量、手机速度和长内容处理已通过验收。

文件为 `qwen2.5-0.5b-instruct-q4_k_m.gguf`，官方实际大小为 491,400,032 字节，约 491 MB / 469 MiB，比此前口头估计的约 400 MB 略大。来源固定在官方仓库 revision `9217f5db79a29953eb74d5343926648285ec7e67`，预期 SHA-256 为 `74a4da8c9fdbcd15bd1f6d01d621410d31c6fc00986f5eb687824e7b93d7a9db`，下载前从官方文件元数据核对。可复用的下载地址、版本、大小、许可和校验值保存在 [模型清单](../models/summary-model.json)，后续更新模型时应一并更新这些信息。

工作区副本保存在 `.local-models/`，该目录已加入 `.gitignore`；手机准备目录为 `Download/Sonfolio/`。模型下载不改变应用设置，不自动启用本地 AI，不启动自动总结，也不会发送用户文字或音频。需要使用时，在“设置”的总结配置中选择“导入 GGUF 模型”，从手机上述目录选择文件，再选择“手机本地 AI”并保存；导入会在应用私有目录另存约 491 MB 副本，当前导入逻辑至少预留 512 MiB 空间，不会删除下载来源或原始录音。

模型文件只在工作区和测试手机保存，公开仓库仅提交清单与说明，不通过 Git LFS 分发这份 GGUF。原有 VAD/ASR 模型和 native 库仍按既定 Git LFS 配置同步，它们与新总结模型的生命周期不同。后续验收应使用固定中文样例检查短对话、结构化决定与安排、较长内容及取消恢复，再讨论是否换用更大的模型；本次下载不改变 0.1.8 记录中的其他待验收项。
