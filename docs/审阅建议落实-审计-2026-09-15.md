# 审阅建议落实审计（2026-09-15）

目的：确认「0.1.8 后审阅建议」在当前源码中的落实情况，并记录本轮补齐。结论基于源码检查与主机验证，不含真机功能复验。

## 项目当前情况

- 版本 `0.2.0`（versionCode 10）；`compileSdk 35`，`minSdk 29`；GPL-3.0-only。
- 0.2.0 迭代本身已作为基线提交到 `main`（`6f2b054`）；本轮审阅建议整在 `feat/review-followup` 分支。
- 主机验证：JVM 单测 `--rerun-tasks` **45 项，0 失败**；`compileDebugAndroidTestKotlin` 通过。
- 真机（TCP ADB）：Redmi Note 8 Pro（Android 10）已装 `0.2.0 (10)`；本轮功能待真机复验。

## 审阅建议落实状态

| # | 审阅建议 | 状态 | 说明 |
|---|---|---|---|
| 1 | 搜索后安静地看命中原文 | 完成 | 命中行定位+高亮、「播放这一句」独立 |
| 2 | 批量导出与清理 | 完成 | 多选、大小、标记保护、zip 导出、独立回听页、还能录约 X 小时 |
| 3 | 录音异常可恢复或明确结束 | 完成 | 尝试恢复 / 结束本次记录 |
| 4 | 首页整理（一日回顾固定位置、后台任务合并可展开） | 完成（本轮） | 一日回顾移到日期栏下方固定位置；处理进度合并为一行、可展开各段状态 |
| 5 | 处理状态统一为可理解文案 | 完成 | 「①→④」阶段文案，VAD_READY 归入等待 |
| 6 | 标记撤销、标题修改、简短备注、修正转写（保留原始版本） | 完成（本轮） | 对话详情页四项；Schema 5 新增 note / originalText |
| 7 | 摘要引用原文、文本导出、AI 设置单独页面 | 完成（本轮） | 重点整理逐条可定位原句；对话文本导出；AI/转文字设置已在设置页独立分区 |
| B | 搜索筛选「日期范围」与「标记」拆开 | 完成（本轮） | `SearchDateRange` + `markedOnly` 两个独立条件 |
| A | 长录音回听倍速 / 前后跳转 / 后台播放 | 不做 | 本轮明确跳过 |

### 同步修复项

| 项 | 状态 | 依据 |
|---|---|---|
| 全量历史重建导致「越用越慢」 | 完成 | `rebuildRegion` 按受影响日期增量整理 |
| 合并会话后旧入口失效 | 完成 | `ConversationAliasEntity` + 别名重定向 |

## 本轮新增/改动

- 数据层：`SearchQuery` 拆分筛选；Schema 5（`conversations.note`、`transcripts.originalText`）+ `MIGRATION_4_5`；`ConversationRepository`/`SonfolioViewModel` 新增标题、备注、撤销标记、修正转写接口。
- 界面：搜索筛选两个独立控件；对话详情「改标题 / 备注 / 取消标记 / 修正这一句 / 导出文本」；摘要要点可定位原句；首页一日回顾固定位置 + 处理卡可展开。
- 测试：`SearchQueryTest` 增加日期范围与标记组合用例；新增 `ConversationEditIntegrationTest`。

以上均为主机侧结论；正式发布门槛仍需按 `docs/开发验收-0.2.0.md` 做对应真机复验。
